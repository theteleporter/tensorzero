{
  "type": "object",
  "properties": {
    "question": {
      "type": "string",
      "description": "The question to be answered."
    },
    "queries_remaining": {
      "type": "integer",
      "description": "The number of queries remaining for the system."
    }
  },
  "required": ["question", "queries_remaining"],
  "additionalProperties": false
}
